import {CommonModule} from '@angular/common';import {ChangeDetectionStrategy,Component,inject,signal} from '@angular/core';import {MatIconModule} from '@angular/material/icon';import {ActivatedRoute} from '@angular/router';import {PublicContractVerification} from '../student.models';import {StudentService} from '../student.service';
@Component({selector:'app-contract-verification',standalone:true,imports:[CommonModule,MatIconModule],templateUrl:'./contract-verification-page.html',styleUrl:'./contract-verification-page.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class ContractVerificationPage{private readonly service=inject(StudentService);private readonly route=inject(ActivatedRoute);readonly result=signal<PublicContractVerification|null>(null);readonly loading=signal(true);readonly notFound=signal(false);constructor(){this.service.verifyPublicContract(this.route.snapshot.paramMap.get('code')??'').subscribe({next:value=>{this.result.set(value);this.loading.set(false)},error:()=>{this.notFound.set(true);this.loading.set(false)}})}}
